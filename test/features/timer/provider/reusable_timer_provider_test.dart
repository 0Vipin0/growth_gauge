import 'package:flutter_test/flutter_test.dart';
import 'package:mockito/mockito.dart';
import 'package:your_project_name/features/timer/provider/reusable_timer_provider.dart';

void main() {
  group('ReusableTimerProvider Tests', () {
    // Add tests for each method in reusable_timer_provider.dart
  });
}